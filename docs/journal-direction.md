# Personal journal direction

Mode: Operate. Code-led; conceptual and UI replacement delegated by the user on 2026-09-08, reaffirmed by “마저 진행해줘”.

THESIS: A person leaves one day's spending, then sees a tangible place for that day in their own week. No other user is necessary.

OWN-WORLD: A compact weekly desk planner translated into a mobile tool. Warm daylight surface, graphite-green text, restrained green ink for actual saved states. No paper texture, ornamental illustrations, fake statistics, gradients, or punitive streak imagery.

STORY: Pick spent / did not spend, enter total if needed, optionally add a note, save, see the week. History and backup are one navigation item away. Public legacy space stays explicitly separate.

FIRST VIEWPORT: Wordmark and discreet existing-record link; functional date; direct question; two equal status controls; local-storage explanation; an actual seven-day strip whose empty cells remain empty. The surface itself performs the job. At desktop width the week sits beside the editor.

FORM: Fixed type scale, familiar semantic controls, one responsive grid. Primary action appears after a status is selected. Checkmark appears only after successful storage. The saved day moves from empty outline to filled green with a single 240ms state transition; reduced motion removes it.

FORM provenance: concept-seed key `4b593a7f`, scope `direction`, mode `operate`, assigned index `7`, approved pool `c3b204a1eed6`. Session command `node /Users/junseungmo/.agents/skills/impeccable/scripts/concept-seed.mjs --scope direction --mode operate` returned these exact values. `build-phase.mjs start --direction 4b593a7f --kind assigned` then returned “choice recorded” and “CODE-LED (from .impeccable config): no comp round and no phase gates.” This is the existing assignment, not a new roll. The grounded weekly planner has no catalog QUALITY BAR card; its explicit contract and actual rendered controls are the review reference.

Risk: This simpler product may still lack enough repeated value. The saved record is local, so explicit device limitation and working export/import are necessary. Existing users must be able to find earlier public work and earned rewards.

Grounded candidates considered: household calendar, pocket spending notebook, bankbook daily rows, morning checklist, receipt archive, habit calendar, weekly desk planner. The direction seed selected candidate 7. The shipped system uses the planner's daily topology, editable totals and persistent dated history.

Challenger judgment: ticket wallet's sequential permanent states donate explicit preserved history but add an irrelevant travel vocabulary; pixel city adds exploration instead of reducing work; fabric transformation adds unnecessary material metaphor; seven-segment instrument promotes amount display over the recording decision. Each loses product clarity for this low-friction task. The resulting planner keeps strong state consistency and stable numeric alignment without taking decorative motifs.

No generated imagery or approved comp. Quality evidence is the rendered mobile/desktop UI and browser interaction tests. Preserve Korean usability, readable muted text and real empty/error states during finishing.
